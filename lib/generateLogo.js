
const { Triangle, Square, Circle } = require ('./shapes.js');


// Do either a switch statement or an if/else statement to make sure you use the right class constructor

//OR var shapeInfo = square.render, etc*


function generateLogo() {
    console.log("This file has been imported correctly");
    console.log(data);

// Check to see data.shape, log the value





    /*
    if (data.shape==="Square") {



    }
    else if (data.shape === "Circle") {

    }
    else {

    }
    */




// Set up if/if else/else statement


// Do a .render using the SVG file structure


// Input the appropriate data.values in the correct locations to produce a valid SVG file

// Return the entire string, which will be set against the "logo" variable


}

// Don't forget to export the function to the index.js

module.exports = {generateLogo}